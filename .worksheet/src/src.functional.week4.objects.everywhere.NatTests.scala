package src.functional.week4.objects.everywhere

import src.functional.week4.objects.everywhere._

object NatTests {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(162); val res$0 = 

  new Successor(Zero).+(new Successor(Zero));System.out.println("""res0: src.functional.week4.objects.everywhere.Successor = """ + $show(res$0))}
  
}
